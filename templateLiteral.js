const pais = "España";
const capital = "Madrid";
const divisa = "Euro";

const mensaje =
  "El pais de: " +
  pais +
  " con la capital: " +
  capital +
  "  la divisa es:" +
  divisa;

console.log(mensaje);

const literal = `El pais de ${pais} con la capital ${capital} 
                 la divisa es ${divisa}`;
console.log(literal);
